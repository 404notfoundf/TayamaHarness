# 数据模型

> 本项目的核心数据模型定义、存储方案。
> **命名约束**：见 `.harness/rules/数据库命名规范.md`；术语见 `.harness/CONTEXT.md`。

## 命名约定（项目摘要）

> 只记**相对规范的例外**（前缀、单复数、时间列名等）及范围。未写则用 `数据库命名规范.md` 默认。

- 表名：
- 时间 / 软删列：
- 其他例外：

## 核心模型

| 模型 | 说明 | 存储 |
|------|------|------|
| <模型名> | <描述> | <MySQL/Redis/…> |

## 实体关系（erDiagram）

```mermaid
erDiagram
  %% 示例：ORDER ||--o{ ORDER_LINE : contains
```

## 表结构

### `<table_name>`

| 列名 | 类型 | 约束 | 默认 | 说明（含 CONTEXT 术语） |
|------|------|------|------|-------------------------|
| id | | PK | | |
| <column> | | | | |

#### ORM 映射

> 约定见 `.harness/rules/ORM与库表映射.md`（语言包注入；无则按本项目习惯成对登记）。

| 库列 | 应用层字段 |
|------|------------|
| | |

## 变更记录

| 日期 | Change ID | 摘要 |
|------|-----------|------|
| | | |

## 存储方案

<描述整体存储架构：关系型数据库、缓存、搜索引擎等>
